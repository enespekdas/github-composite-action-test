name: 'File Copy'
description: 'Pretends to copy some files and return the number of files copied'
inputs:
  destinationFolder:  # path
    description: 'The folder to copy the files to'
    required: true
    default: '~'
outputs:
  copied-files: 
    description: "Number of files copied"
    value: "test"
runs:
  using: "composite"
  steps: 
    - name: 'podman yukleniyor'
      run: podman --help
      shell: bash
    